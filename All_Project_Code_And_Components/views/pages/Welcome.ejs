<!DOCTYPE html>
<%- include ('../partials/header') %>
<html>
<head>
    <title>Welcome</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
</head>
<body>
    <header>
        <!-- Existing header content... -->
    </header>

    <div class="container mt-4">
        <script>
            var currentHour = new Date().getHours();
            var greeting;
            if (currentHour < 12) {
                greeting = "Good Morning";
            } else if (currentHour < 18) {
                greeting = "Good Afternoon";
            } else {
                greeting = "Good Evening";
            }
        </script>
    <h2><script>document.write(greeting)</script>, <%= username %> </h2>
        
        <!-- Search Form -->
        <div class="search-books mt-4">
            <h2>Search for a Book</h2>
            <form action="/search-books" method="GET">
                <div class="mb-3">
                    <input type="text" class="form-control" name="query" placeholder="Enter book title or author">
                </div>
                <button type="submit" class="btn btn-primary">Search</button>
            </form>
        </div>

        <!-- Read Reviews Link -->
        <div class="read-reviews mt-4">
            <h2>Read Book Reviews</h2>
            <a href="/reviews" class="btn btn-secondary">View Reviews</a>
        </div>

        <!-- Add Review Link -->
        <div class="add-review mt-4">
            <h2>Add a New Review</h2>
            <a href="/add_reviews" class="btn btn-success">Add Review</a>
        </div>
    </div>
</body>
</html>
<%- include ('../partials/footer') %>